#include "LinkedList.h"
#include <iostream>
#include <cassert>


using namespace std;


int main() {


    return 0;
    /*
    LinkedList ls;
    ls.insertToFront("The Mandalorian");
    ls.insertToFront("Baby Yoda");
    ls.insertToFront("Cara Dune");
    ls.insertToFront("Greef Karga");
    ls.printList();
    ls.printReverse();
    */

    /*
    LinkedList list;
    list.insertToFront("Apple");
    list.insertToFront("Banana");
    list.insertToFront("Cherry");
    


    /*
    string item;
    if (list.get(0, item))
        cout << "Element at index 0: " << item << endl;  // Expected output: Element at index 0: Cherry
    else
        cout << "Index out of range." << endl;

    if (list.get(2, item))
        cout << "Element at index 2: " << item << endl;  // Expected output: Element at index 2: Apple
    else
        cout << "Index out of range." << endl;

    if (list.get(5, item))
        cout << "Element at index 5: " << item << endl;
    else
        cout << "Index out of range." << endl;  // Expected output: Index out of range.
     */

    
   


    /*
    LinkedList list1;
    list1.insertToFront("Apple");
    list1.append(list1);
    list1.printList();

    assert(list1.size() == 2);

    LinkedList list2;
    list2.insertToFront("Apple");
    list2.insertToFront("Strawberry");
    list2.insertToFront("Cherry");

    list1.append(list2);
    assert(list1.size() == 5);
    assert(list2.size() == 3);
    list1.printList();
    */
    


}